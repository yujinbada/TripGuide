package com.example.tripguide

import android.view.View
import androidx.recyclerview.widget.RecyclerView

class Recmd_viewholder(itemView: View): RecyclerView.ViewHolder(itemView) {
    val TAG: String = "로그"

    private val r
}